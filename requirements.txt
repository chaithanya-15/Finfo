# Core libraries
pandas>=2.0.0
requests>=2.28.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# PDF processing
pypdf>=3.0.0
pdfplumber>=0.10.0
unstructured[pdf]>=0.6.0

# Embeddings and vector stores
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
chromadb>=0.4.0
langchain>=0.0.300
langchain-community>=0.0.10
llama-index>=0.9.0

# Transformers and LLMs
transformers>=4.30.0
accelerate>=0.20.0
# llama.cpp runs the 4-bit GGUF generator. bitsandbytes has no macOS build, so it is not
# usable for quantised local inference here.
# Build with: CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python
llama-cpp-python>=0.3.0

# Evaluation
ragas>=0.1.0
datasets>=2.0.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Utilities
rapidfuzz>=3.0.0
tiktoken>=0.5.0
tabulate>=0.9.0
pytest>=7.0.0