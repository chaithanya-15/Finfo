# Core
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0
pyyaml>=6.0
tqdm>=4.65.0

# PDF processing
pypdf>=3.0.0
pdfplumber>=0.10.0
# The "unstructured" extractor (extract_method="unstructured") is optional and heavy; it is
# not needed for the default pdfplumber path. Install separately if you want it:
#   pip install "unstructured[pdf]"

# Embeddings and vector stores
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
chromadb>=0.4.0

# Generation: 4-bit/8-bit GGUF weights through llama.cpp. bitsandbytes has no macOS build, so
# it is not usable for quantised local inference. Build llama.cpp for the local accelerator:
#   CMAKE_ARGS="-DGGML_METAL=on"  pip install llama-cpp-python   # Apple silicon (Metal)
#   CMAKE_ARGS="-DGGML_CUDA=on"   pip install llama-cpp-python   # NVIDIA GPU
#   pip install llama-cpp-python                                  # CPU
llama-cpp-python>=0.3.0

# Evaluation
rouge-score>=0.1.2
scikit-learn>=1.3.0
rapidfuzz>=3.0.0
rank-bm25>=0.2.2
matplotlib>=3.5.0
seaborn>=0.11.0
tabulate>=0.9.0

# Tokenisation and tests
tiktoken>=0.5.0
pytest>=7.0.0
